product ERP on the Net

table poreq
  caption "Requisition Entry"
  columns ztime,zutime,zid,xporeqnum,xdate,xsup,xorg,xsupref,xdatesupref,xdiv,xsec,xproj,~
          xstatusreq,xporef,xcur,xexch,xhmsg,xfmsg,xrev,xref,xsp
  primary key zid, xporeqnum
  foreign key
        zid references zbusiness.zid
  end foreign key
  check
    //country  = 'US' or country = 'SG'
  end check
  //field xcus
    //extend
    //attrib trn Sales Order, 10
  //end field
  //sql
end table
