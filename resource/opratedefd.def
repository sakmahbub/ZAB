product ZAB ERP

table opratedefd
  caption "Sales Rate Defaults"
  columns ztime,zutime,zauserid,zuuserid,zid,~
          xgrade,xrow,xacc,xaccvat,xaccdisc,xacccr,xcomm,xcomm1,xcomm2,xcomm3,xcomparison

  primary key zid,xgrade,xrow
  foreign key
        zid references zbusiness.zid
		  zid,xgrade references opratedefh.zid,xgrade
  end foreign key

end table
