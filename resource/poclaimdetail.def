product ERP on the Net

table poclaimdetail
  caption "Claim Detail"
  columns ztime,zutime,zid,zemail,~
          xclaimnum,xrow, xitem,xquality,xlong, ~
          xqtyord,xqtydel,xrate,xunit,xprime,xbase,xwh,xdiscf,xdisc,xcomm

  primary key zid,xclaimnum,xrow

  foreign key
        zid references zbusiness.zid
        zid,xclaimnum references poclaim.zid,xclaimnum
        zid,xitem references caitem.zid,xitem
  end foreign key

end table
