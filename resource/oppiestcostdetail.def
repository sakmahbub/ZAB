product ZAB ERP

table oppiestcostdetail
  caption "MO PI Estimated Cost Detail"
  columns  ztime,zutime,zauserid,zuuserid, zid, xordernum,xrow,xitemrow,xitem,xqtyord,xrate,xlineamt,xnote,xqtycom
           
           
  primary key zid, xordernum,xrow,xitemrow
  
  foreign key
        zid references zbusiness.zid
        zid,xordernum references opordheader.zid,xordernum
	zid,xordernum,xrow references oppiestcostheader.zid,xordernum,xrow
        zid,xitem references caitem.zid,xitem
  end foreign key
end table
